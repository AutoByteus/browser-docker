# Requirements

- Keep Python 3.13 in the browser base image.
- Install a Supervisor release compatible with Python 3.13.
- Ensure the base entrypoint launches the compatible Supervisor executable.
- Validate the complete Supervisor configuration before publishing a new base image.
