# Release Notes

- Release version: `1.3.7`
- Upgrade the default Python runtime from 3.11 to 3.13.
- Install the GitHub CLI (`gh`) in the base image.
- Keep websockify aligned with the Python 3.13 installation.
- Route Chromium startup through the configurable launcher script used by the image.
